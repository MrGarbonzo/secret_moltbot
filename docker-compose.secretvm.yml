services:
  agent:
    image: ghcr.io/mrgarbonzo/secret_moltbot/agent:52e6c2b1f70b4728b3b6023ec08160449a45890e
    container_name: secretmolt-agent
    ports:
      - "8000:8000"
    environment:
      - SECRET_AI_API_KEY=${SECRET_AI_API_KEY}
      - SECRET_AI_MODEL=${SECRET_AI_MODEL:-DeepSeek-R1-70B}
      - USE_MOCK_MOLTBOOK=${USE_MOCK_MOLTBOOK:-true}
      - AGENT_NAME=testerfinal222
      - AGENT_DESCRIPTION=testing1234
      - HEARTBEAT_INTERVAL_HOURS=${HEARTBEAT_INTERVAL_HOURS:-4}
      - POSTS_PER_DAY=${POSTS_PER_DAY:-2}
      - DATA_DIR=/data
      - DB_PATH=/data/memory.db
    volumes:
      - agent-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  dashboard:
    image: ghcr.io/mrgarbonzo/secret_moltbot/dashboard:52e6c2b1f70b4728b3b6023ec08160449a45890e
    container_name: secretmolt-dashboard
    ports:
      - "3000:3000"
    environment:
      - AGENT_URL=http://agent:8000
    depends_on:
      agent:
        condition: service_healthy
    restart: unless-stopped

volumes:
  agent-data:
    driver: local
